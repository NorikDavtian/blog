doctype
html(lang=config.language)
    head
        include head
    body
        header
            a.logo-link(href=site.data.menu.header.home)
                img(src=site.data.settings.logo)
            include nav
        section.container
            block container
        footer
            block pagination
            include footer
        include scripts