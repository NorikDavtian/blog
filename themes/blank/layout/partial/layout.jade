doctype
html(lang=config.language)
    head
        include head
    body
        header
            a.logo-link(href=url_for())
                img(src=site.data.settings.logo)
            include nav
        section.container
            block container
        footer
            block pagination
            include footer
        include scripts